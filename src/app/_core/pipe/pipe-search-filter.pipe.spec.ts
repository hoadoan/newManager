import { PipeSearchFilterPipe } from './pipe-search-filter.pipe';

describe('PipeSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
